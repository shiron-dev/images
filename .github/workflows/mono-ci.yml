name: Monorepo CI
on:
  pull_request:
    branches:
      - main
jobs:
  detect-changes:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      changed_dirs: ${{ steps.changed-dirs.outputs.changed_dirs }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get changed directories
        id: changed-dirs
        run: |
          CHANGED_FILES=$(git diff --name-only origin/${{ github.event.pull_request.base.ref }}..${{ github.event.pull_request.head.sha }})
          CHANGED_DIRS=""
          while IFS= read -r FILE; do
            DIR=$(echo "$FILE" | cut -d/ -f1)
            if [ -d "$DIR" ]; then
              CHANGED_DIRS="$CHANGED_DIRS$DIR\n"
            fi
          done <<< "$CHANGED_FILES"
          echo -e "$CHANGED_DIRS" | uniq | jq -Rsc 'split("\n")[0:-1]' >> input.json
          CHANGED_DIRS=$(cat input.json release-please-config.json \
            | jq -s -r '(.[1].packages | keys) + .[0] | .[]' \
            | sort | uniq -d | jq -Rsc 'split("\n")[0:-1]')
          echo "changed_dirs=$CHANGED_DIRS" >> "$GITHUB_OUTPUT"
          cat "$GITHUB_OUTPUT"
  docker:
    needs: detect-changes
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        changed_dir: ${{ fromJson(needs.detect-changes.outputs.changed_dirs) }}
    outputs:
      changed_dir: ${{ matrix.changed_dir }}
      dockerfiles: ${{ steps.get-dockerfiles.outputs.dockerfiles }}
    defaults:
      run:
        working-directory: ${{ matrix.changed_dir }}
    steps:
      - uses: actions/checkout@v4
      - name: Get Dockerfiles
        id: get-dockerfiles
        run: |
          DOCKERFILES=$(find . -maxdepth 1 -name "Dockerfile.*" | sort | jq -Rsc 'split("\n")[0:-1]')
          echo "dockerfiles=$DOCKERFILES" >> "$GITHUB_OUTPUT"
          cat "$GITHUB_OUTPUT"
  docker-build:
    needs: docker
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        dockerfile: ${{ fromJson(needs.docker.outputs.dockerfiles) }}
    defaults:
      run:
        working-directory: ${{ needs.docker.outputs.changed_dir }}
    name: docker-build ${{ matrix.dockerfile }} (${{ needs.docker.outputs.changed_dir }})
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Build Docker images
        run: ../scripts/build_dockerfile.sh ${{ matrix.dockerfile }}
