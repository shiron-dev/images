[project]
name = "discord-webhook"
version = "0.1.0"
description = "Discord bot that forwards messages to webhooks"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.0",
    "requests>=2.32.4",
]
