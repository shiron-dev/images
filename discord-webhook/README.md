# discord-webhook

## Environment variables

- `DISCORD_BOT_TOKEN`: Discord bot token used to connect the bot.
- `WEBHOOK_URLS`: Comma-separated list of destination webhook URLs.

Example:

```bash
DISCORD_BOT_TOKEN=your_discord_bot_token
WEBHOOK_URLS=https://example.com/webhook/1,https://example.com/webhook/2
```
